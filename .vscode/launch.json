{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Package",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}",
            "env": {
                "PROJECT_ROOT": "${workspaceFolder}",
                "PATH": "/usr/local/go/bin:${env:PATH}",
                "GOPATH": "${env:GOPATH}"
            },
            "args": []
        }
    ]
}
